<%= render 'shared/modal_booking' %>


<div class="boat-pic" style="background-image: linear-gradient(-225deg, rgba(0,0,0,0.6) 0%, rgba(0,0,0,0.3) 50%), url(<%= @university.photo %>)">
  <div class="header-trip">
    <div class="header-trip-captain text-center padded">
      <p><%= @university.pays.capitalize %></p>
    </div>
    <div>
      <h1>
        <%= @university.city %>
      </h1>
      <p><%= @university.course.capitalize %></p>

    </div>
    <button type="button" class="navbar-wagon-item navbar-wagon-button btn btn-danger" data-toggle="modal" data-target="#myModal">
      Postuler dans cette université
    </button>
</div>
</div>
